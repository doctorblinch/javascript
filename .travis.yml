language: node_js
node_js:
  - "10"
before_cript:
  - npm  ci
script:
  - npm run lint
  - npm run test
  - npm run build
  - npm run deploy
  - npm run getToken
  - now --token=$ZEIT_KEY -e BOT_TOKEN=$BOT_TOKEN
